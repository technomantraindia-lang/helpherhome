@extends('layouts.admin')
@section('title','Worker Documents')
@section('heading','Worker Documents')
@section('subheading','Review uploaded worker documents through authorized links.')
@section('content')
<form class="card mb-5 flex flex-wrap gap-3 p-4" method="GET"><input name="search" placeholder="Worker name, code, document number" value="{{ request('search') }}"><select name="status"><option value="">All statuses</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status') === $status->value)>{{ str($status->value)->headline() }}</option>@endforeach</select><select name="type"><option value="">All types</option>@foreach($types as $type)<option value="{{ $type }}" @selected(request('type') === $type)>{{ str($type)->headline() }}</option>@endforeach</select><button class="btn-primary">Filter</button></form>
<div class="card overflow-x-auto"><table class="w-full min-w-[850px] text-left text-sm"><thead><tr><th class="p-3">Worker</th><th>Document</th><th>Type</th><th>Verification</th><th>Actions</th></tr></thead><tbody class="divide-y">@forelse($documents as $document)<tr><td class="p-3">{{ $document->worker?->name }}<div class="text-xs text-neutral-500">{{ $document->worker?->worker_code }}</div></td><td>{{ $document->document_name ?: 'Unnamed document' }}</td><td>{{ str($document->document_type)->headline() }}</td><td>{{ str($document->verification_status->value)->headline() }}</td><td><a class="font-bold text-gold-600" href="{{ route('admin.worker-documents.view',$document) }}">View</a> · <a class="font-bold text-gold-600" href="{{ route('admin.worker-documents.download',$document) }}">Download</a></td></tr>@empty<tr><td colspan="5" class="p-8 text-center text-neutral-500">No worker documents found.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $documents->links() }}</div>
@endsection
