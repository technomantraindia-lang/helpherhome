@extends('layouts.admin')
@section('title','Assignments') @section('heading',$scope==='active'?'Active Assignments':($scope==='history'?'Assignment History':'All Assignments')) @section('subheading','Worker placements with complete lifecycle history.')
@section('content')<form class="card mb-6 grid gap-3 p-4 sm:grid-cols-2 lg:grid-cols-5" method="GET"><input name="search" value="{{ request('search') }}" placeholder="Code, customer, worker, mobile"><select name="status"><option value="">All statuses</option>@foreach($statuses as $s)<option value="{{ $s->value }}" @selected(request('status')===$s->value)>{{ $s->label() }}</option>@endforeach</select><select name="service"><option value="">All services</option>@foreach($services as $s)<option value="{{ $s->id }}" @selected(request('service')==$s->id)>{{ $s->name }}</option>@endforeach</select><select name="duty_type"><option value="">All duty types</option>@foreach($dutyTypes as $d)<option value="{{ $d->id }}" @selected(request('duty_type')==$d->id)>{{ $d->name }}</option>@endforeach</select><div class="flex gap-2"><button class="btn-primary flex-1">Filter</button><a class="btn-secondary" href="{{ url()->current() }}">Clear</a></div><input type="date" name="start_date" value="{{ request('start_date') }}"></form><div class="card overflow-x-auto"><table class="w-full min-w-[1000px] text-left text-sm"><thead class="bg-neutral-50"><tr>@foreach(['Assignment Code','Customer','Worker','Service','Duty Type','Start','End','Salary','Status','Actions'] as $h)<th class="px-4 py-3">{{ $h }}</th>@endforeach</tr></thead><tbody class="divide-y">@forelse($assignments as $a)<tr><td class="px-4 py-4 font-bold">{{ $a->assignment_code }}</td><td class="px-4">{{ $a->customer->name }}</td><td class="px-4">{{ $a->worker->name }}</td><td class="px-4">{{ $a->service->name }}</td><td class="px-4">{{ $a->dutyType?->name ?: '—' }}</td><td class="px-4">{{ $a->assignment_start_date->format('d M Y') }}</td><td class="px-4">{{ $a->assignment_end_date?->format('d M Y') ?: 'Open' }}</td><td class="px-4">{{ $a->monthly_salary?'₹'.number_format((float)$a->monthly_salary):'—' }}</td><td class="px-4"><span class="badge-neutral">{{ $a->status->label() }}</span></td><td class="px-4"><a class="font-bold text-gold-500" href="{{ route('admin.assignments.show',$a) }}">View</a></td></tr>@empty<tr><td colspan="10" class="p-10 text-center text-neutral-500">No assignments found.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $assignments->links() }}</div>@endsection
