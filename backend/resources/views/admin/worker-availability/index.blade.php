@extends('layouts.admin')
@section('title','Worker Availability')
@section('heading','Worker Availability')
@section('subheading','Review and update current worker availability.')
@section('content')
<form class="card mb-5 flex flex-wrap gap-3 p-4" method="GET"><input name="search" placeholder="Name or worker code" value="{{ request('search') }}"><select name="status"><option value="">All statuses</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status') === $status->value)>{{ str($status->value)->headline() }}</option>@endforeach</select><button class="btn-primary">Filter</button></form>
<div class="card overflow-x-auto"><table class="w-full min-w-[700px] text-left text-sm"><thead><tr><th class="p-3">Worker</th><th>Services</th><th>Current status</th><th>Update</th></tr></thead><tbody class="divide-y">@forelse($workers as $worker)<tr><td class="p-3"><a class="font-bold" href="{{ route('admin.workers.show',$worker) }}">{{ $worker->name }}</a><div class="text-xs text-neutral-500">{{ $worker->worker_code }}</div></td><td>{{ $worker->services->pluck('name')->join(', ') ?: '—' }}</td><td>{{ str($worker->availability_status->value)->headline() }}</td><td><form method="POST" action="{{ route('admin.worker-availability.update',$worker) }}">@csrf @method('PATCH')<select name="availability_status">@foreach($statuses as $status)<option value="{{ $status->value }}" @selected($worker->availability_status === $status)>{{ str($status->value)->headline() }}</option>@endforeach</select><button class="ml-2 font-bold text-gold-600">Save</button></form></td></tr>@empty<tr><td colspan="4" class="p-8 text-center text-neutral-500">No workers found.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $workers->links() }}</div>
@endsection
