@extends('layouts.admin')
@section('title','Shortlisted Workers') @section('heading','Shortlisted Workers') @section('subheading','Preserved candidate history for every customer requirement.')
@section('content')<form class="card mb-5 flex gap-3 p-4" method="GET"><select name="status"><option value="">All statuses</option>@foreach($statuses as $s)<option value="{{ $s->value }}" @selected(request('status')===$s->value)>{{ $s->label() }}</option>@endforeach</select><button class="btn-primary">Filter</button></form><div class="card overflow-x-auto"><table class="w-full min-w-[900px] text-left text-sm"><thead class="bg-neutral-50"><tr>@foreach(['Requirement','Customer','Worker','Service','Shortlisted Date','Status','Actions'] as $h)<th class="px-4 py-3">{{ $h }}</th>@endforeach</tr></thead><tbody class="divide-y">@forelse($items as $item)<tr><td class="px-4 py-4"><a href="{{ route('admin.customer-requirements.show',$item->requirement) }}">{{ $item->requirement->requirement_code }}</a></td><td class="px-4">{{ $item->requirement->customer->name }}</td><td class="px-4">{{ $item->worker->name }}<small class="block text-neutral-500">{{ $item->worker->worker_code }}</small></td><td class="px-4">{{ $item->requirement->service->name }}</td><td class="px-4">{{ $item->shortlisted_at?->format('d M Y') }}</td><td class="px-4">{{ $item->status->label() }}</td><td class="px-4"><div class="flex items-center gap-2"><a href="{{ route('admin.workers.show',$item->worker) }}">View</a><a class="font-bold text-gold-500" href="{{ route('admin.assignments.create',[$item->requirement,$item->worker]) }}">Select</a><form class="flex gap-2" method="POST" action="{{ route('admin.worker-shortlists.update',$item) }}">@csrf @method('PATCH')<select class="py-1" name="status">@foreach($statuses as $s)<option value="{{ $s->value }}" @selected($item->status===$s)>{{ $s->label() }}</option>@endforeach</select><button class="btn-secondary py-1">Save</button></form></div></td></tr>@empty<tr><td colspan="7" class="p-10 text-center">No shortlisted workers.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $items->links() }}</div>@endsection
