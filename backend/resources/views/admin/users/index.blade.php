@extends('layouts.admin')
@section('title','Users') @section('heading','Users') @section('subheading','Control portal access, roles, and account status.')
@section('actions') @if(auth()->user()->hasPermission('users.create'))<a href="{{ route('admin.users.create') }}" class="btn-primary">+ Add user</a>@endif @endsection
@section('content')
<div class="card overflow-hidden"><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="border-b border-neutral-200 bg-neutral-50 text-xs uppercase tracking-wider text-neutral-500"><tr><th class="px-6 py-4">User</th><th class="px-6 py-4">Mobile</th><th class="px-6 py-4">Role</th><th class="px-6 py-4">Status</th><th class="px-6 py-4">Last login</th><th class="px-6 py-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-neutral-100">
@foreach($users as $user)<tr><td class="px-6 py-4"><p class="font-bold text-neutral-900">{{ $user->name }}</p><p class="text-xs text-neutral-500">{{ $user->email }}</p></td><td class="px-6 py-4 text-neutral-600">{{ $user->mobile ?: '—' }}</td><td class="px-6 py-4">{{ $user->role?->name ?: 'Unassigned' }}</td><td class="px-6 py-4"><span class="{{ $user->is_active ? 'badge-active' : 'badge-inactive' }}">{{ $user->is_active ? 'Active' : 'Inactive' }}</span></td><td class="px-6 py-4 text-neutral-500">{{ $user->last_login_at?->diffForHumans() ?? 'Never' }}</td><td class="px-6 py-4 text-right">@if(auth()->user()->hasPermission('users.edit') && (! $user->hasRole('super-admin') || auth()->user()->hasRole('super-admin')))<a class="font-bold text-gold-500 hover:underline" href="{{ route('admin.users.edit',$user) }}">Edit</a>@endif</td></tr>@endforeach
</tbody></table></div>@if($users->hasPages())<div class="border-t border-neutral-100 px-6 py-4">{{ $users->links() }}</div>@endif</div>
@endsection
