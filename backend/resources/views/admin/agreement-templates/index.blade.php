@extends('layouts.admin')
@section('title','Agreement Templates') @section('heading','Agreement Templates') @section('subheading','Default terms are copied into each agreement and do not alter old snapshots.')
@section('content')<div class="grid gap-5 lg:grid-cols-2">@foreach($templates as $template)<article class="card p-6"><div class="flex justify-between"><div><h2 class="font-black">{{ $template->name }}</h2><p class="text-sm text-neutral-500">{{ $template->title }}</p></div>@if($template->is_default)<span class="badge-gold">Default</span>@endif</div><p class="mt-4 line-clamp-4 whitespace-pre-line text-sm">{{ $template->intro_text }}</p>@if(auth()->user()->hasPermission('agreement-templates.edit'))<a class="btn-secondary mt-4" href="{{ route('admin.agreement-templates.edit',$template) }}">Edit Template & Terms</a>@endif</article>@endforeach</div>@endsection
