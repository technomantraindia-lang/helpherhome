@extends('layouts.admin')
@section('title','Replacement Requests') @section('heading','Replacement Requests') @section('subheading','Replacement assistance with preserved assignment history.')
@section('content')<form class="card mb-6 grid gap-3 p-4 sm:grid-cols-2 lg:grid-cols-5" method="GET"><input name="search" value="{{ request('search') }}" placeholder="Code, customer, old worker"><select name="status"><option value="">All statuses</option>@foreach($statuses as $s)<option value="{{ $s->value }}" @selected(request('status')===$s->value)>{{ $s->label() }}</option>@endforeach</select><select name="service"><option value="">All services</option>@foreach($services as $s)<option value="{{ $s->id }}" @selected(request('service')==$s->id)>{{ $s->name }}</option>@endforeach</select><input type="date" name="date" value="{{ request('date') }}"><button class="btn-primary">Filter</button></form><div class="card overflow-x-auto"><table class="w-full min-w-[950px] text-left text-sm"><thead class="bg-neutral-50"><tr>@foreach(['Replacement Code','Customer','Old Worker','Service','Reason','Request Date','Required Date','Status','New Worker','Actions'] as $h)<th class="px-4 py-3">{{ $h }}</th>@endforeach</tr></thead><tbody class="divide-y">@forelse($items as $r)<tr><td class="px-4 py-4 font-bold">{{ $r->replacement_code }}</td><td class="px-4">{{ $r->customer->name }}</td><td class="px-4">{{ $r->oldWorker->name }}</td><td class="px-4">{{ $r->assignment->service->name }}</td><td class="px-4">{{ str($r->reason)->headline() }}</td><td class="px-4">{{ $r->requested_date->format('d M Y') }}</td><td class="px-4">{{ $r->replacement_required_date?->format('d M Y') ?: '—' }}</td><td class="px-4">{{ $r->status->label() }}</td><td class="px-4">{{ $r->newWorker?->name ?: 'Pending' }}</td><td class="px-4"><a class="font-bold text-gold-500" href="{{ route('admin.replacements.show',$r) }}">View</a></td></tr>@empty<tr><td colspan="10" class="p-10 text-center">No replacement requests.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $items->links() }}</div>@endsection
