@extends('layouts.admin')
@section('title','Add Enquiry') @section('heading','Add Enquiry') @section('subheading','Capture a phone, WhatsApp, social, or manual enquiry.')
@section('content')<form class="card grid gap-4 p-6 md:grid-cols-2" method="POST" action="{{ route('admin.enquiries.store') }}">@csrf<input name="name" required placeholder="Full name"><input name="mobile_number" required placeholder="Mobile number"><input type="email" name="email" placeholder="Email"><input name="city" placeholder="City"><input name="area" placeholder="Area"><select name="service_id"><option value="">Service</option>@foreach($services as $service)<option value="{{ $service->id }}">{{ $service->name }}</option>@endforeach</select><select name="duty_type_id"><option value="">Duty type</option>@foreach($dutyTypes as $duty)<option value="{{ $duty->id }}">{{ $duty->name }}</option>@endforeach</select><input type="date" name="preferred_start_date"><textarea class="md:col-span-2" name="message" rows="4" placeholder="Requirement"></textarea><button class="btn-primary md:col-span-2">Save Enquiry</button></form>@endsection
